Generate keys with:
LINUX: openssl genrsa -out certs/private.pem 4096 && openssl rsa -in certs/private.pem -pubout -out certs/public.pem
WINDOWS: ssh-keygen -t rsa -b 4096 -m PEM -f certs/private.pem; ssh-keygen -f certs/private.pem -e -m PEM > certs/public.pem